#!/bin/bash

# A script to display a series of numbers using while.

counter=1
#'gt' means greater than, 'le' means less than
until [[ "$counter" -gt 10 ]]; do
    echo "The counter is at: $counter"
    counter=$((counter + 1))
done
echo "The count has finished."
